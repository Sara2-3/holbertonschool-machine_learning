# Supervised Learning

Supervised learning is a core branch of machine learning where models are trained on labeled datasets. The algorithm learns the mapping between input features and known outputs, enabling it to make accurate predictions on new, unseen data.

This approach is widely used in tasks such as classification, regression, and forecasting, and serves as the foundation for many real-world applications including fraud detection, image recognition, and natural language processing.
