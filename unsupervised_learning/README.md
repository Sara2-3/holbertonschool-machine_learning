# Dimensionality Reduction Project

This project implements Principal Component Analysis (PCA) in Python using NumPy.
It reduces dataset dimensions while preserving most variance, tested with sample data and reconstruction error checks.
